const express = require('express');
const router = express.Router();

// Placeholder routes - will be implemented later
router.post('/', (req, res) => {
  res.json({ message: 'Contact form endpoint - Coming soon' });
});

module.exports = router;
